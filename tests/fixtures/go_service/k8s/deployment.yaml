apiVersion: apps/v1
kind: Deployment
metadata:
  name: agentpack-fixture
spec:
  template:
    spec:
      containers:
        - name: api
          image: example/api:latest
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
