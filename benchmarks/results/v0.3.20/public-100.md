# v0.3.20 Public 100+ Benchmark

Run:

```bash
agentpack benchmark --public-suite --reproduce v0.3.20
```

The configured suite currently contains 128 cases. Publish the generated table here after running the reproducible suite from a clean checkout.

The existing pinned smoke result remains in [`../2026-06-11-public.md`](../2026-06-11-public.md).
